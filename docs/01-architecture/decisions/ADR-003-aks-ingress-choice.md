# ADR-003: AKS ingress choice

## Context
Expose app endpoints securely in AKS.

## Decision
Use a standard Ingress controller (NGINX or Azure Application Gateway Ingress) based on constraints.

## Consequences
Ingress configuration affects security posture and monitoring.

