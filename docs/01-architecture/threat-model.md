# Threat Model (high level)

- Identity threats (credential theft, privilege escalation)
- Network threats (exposed ports, lateral movement)
- Data threats (unauthorized DB access, leakage)
- Platform threats (misconfiguration, weak secrets handling)

Mitigations mapped to: Entra ID, RBAC, Key Vault, NSGs, Defender for Cloud, least privilege, logging/alerts.

