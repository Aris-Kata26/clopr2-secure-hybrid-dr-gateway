# Example env file — copy to .env and fill in real values (never commit .env)
APP_ENV=dev
APP_PORT=8000

# PostgreSQL — always use the Keepalived VIP, never a direct node IP
DB_HOST=10.0.96.10
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=changeme

