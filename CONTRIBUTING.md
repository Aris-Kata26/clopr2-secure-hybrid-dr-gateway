# Contributing

This repository follows a simple workflow:
- Create a branch
- Open a PR
- CI must pass (Terraform fmt/validate + Ansible lint + YAML checks)

Naming:
- Branch: feature/<topic> or fix/<topic>
- Commits: imperative, short (e.g., "Add AKS base manifests")

